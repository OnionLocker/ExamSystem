# ✅ 最终验证清单

## 🌐 访问地址
```
http://106.53.94.27:5173
```

## 📋 验证步骤

### 1. 查看知识债（核心功能）

**操作：**
1. 打开浏览器访问 `http://106.53.94.27:5173`
2. 登录系统
3. 进入「知识点」页面
4. 点击顶部「知识债」页签

**预期结果：**
- ✅ 看到债务列表（应该有87笔未清债务）
- ✅ 背景色为暖色调（不是纯白色）
- ✅ 每条债务显示：
  - 考点名称
  - 累计错误次数
  - 连对进度（如：0/2）
  - 最近错误日期
  - 掌握度百分比
- ✅ 每条债务有「让 Hermes 出这个考点的题」按钮

### 2. Hermes智能出题

**操作：**
1. 在债务列表中，点击任意债务的出题按钮
2. 观察是否跳转到 Hermes 页面

**预期结果：**
- ✅ 自动跳转到 Hermes 页面
- ✅ 输入框中自动填入结构化指令：
  ```
  #出题清债#
  考点: [具体考点名称]
  模块: [模块名称]
  累计错误: [N]次
  连对进度: [0/2]
  掌握度: [X]%
  
  请针对该考点出5道题，难度分布：easy 2题, mid 2题, hard 1题。
  题目要覆盖该考点的不同变式，不要重复同一场景只换数字。
  ```
- ✅ 可以直接发送或修改指令

### 3. 交卷后复盘提醒

**操作：**
1. 做一套AI练题（任意批次）
2. 交卷后查看成绩页

**预期结果：**
- ✅ 看到成绩卡片（对了X/总数道）
- ✅ 如果有未复盘的题，显示橙色警告卡片：
  - 标题：「这场还有 N 道题的证据没写」
  - 说明：为什么要复盘
  - 按钮：「让 Hermes 复盘这场」
- ✅ 点击按钮能跳转到 Hermes

### 4. 知识点页面背景色

**操作：**
1. 在知识点页面的「知识树」页签查看

**预期结果：**
- ✅ 整体背景色为 `#e8d5b0`（暖褐色）
- ✅ 卡片背景为 `#fdfbf7`、`#faf6ec` 等米白/奶黄色
- ✅ 不是纯白色 `#ffffff`

## 🐛 如果功能不正常

### 问题1：看不到知识债页签
**可能原因：**
- 浏览器缓存未更新
- 需要强制刷新

**解决方案：**
```
Ctrl + Shift + R (Windows/Linux)
Cmd + Shift + R (Mac)
或者清除浏览器缓存
```

### 问题2：知识债列表为空
**可能原因：**
- 数据库中确实没有债务数据
- 需要先做题产生错误

**解决方案：**
```bash
# 检查数据库
sqlite3 /home/ubuntu/ExamSystem/data/exam.db \
  "SELECT COUNT(*) FROM kaodian_debts WHERE mastered = 0"
# 应该返回 87
```

### 问题3：点击出题按钮没反应
**可能原因：**
- JavaScript错误
- 路由配置问题

**解决方案：**
1. 打开浏览器开发者工具（F12）
2. 查看 Console 标签是否有错误
3. 查看 Network 标签确认API调用

### 问题4：背景色还是白色
**可能原因：**
- 显示器色温设置
- 浏览器主题影响

**解决方案：**
1. 检查浏览器是否开启了"强制深色模式"
2. 检查显示器色温设置
3. 使用开发者工具检查实际CSS值

## 📊 数据确认

### 数据库检查命令

```bash
# 进入服务器
cd /home/ubuntu/ExamSystem

# 检查知识债总数
sqlite3 data/exam.db "SELECT COUNT(*) FROM kaodian_debts WHERE mastered = 0"
# 应该返回：87

# 查看Top 5债务
sqlite3 data/exam.db "SELECT kaodian, wrong_count, recovery_streak FROM kaodian_debts WHERE mastered = 0 ORDER BY wrong_count DESC LIMIT 5"

# 应该看到：
# 数量关系-数学运算-排列组合问题-位置限制与排队|32|0
# 数量关系-数学运算-概率问题-定位法与同组概率|30|0
# ...
```

## 🔧 服务器状态确认

```bash
# 检查服务器进程
ps aux | grep "server/index.js" | grep -v grep

# 检查构建文件时间戳
ls -lh /home/ubuntu/ExamSystem/dist/assets/index-*.js
# 应该显示最新时间（2026-09-23 14:57）

# 检查服务器日志
tail -50 /tmp/exam-server.log
```

## 📝 Git状态

```bash
cd /home/ubuntu/ExamSystem
git log --oneline -5

# 应该看到：
# c7bb074 chore: 更新package文件和HermesChat优化
# 051966c docs: 添加快速启动指南
# 931ee0c docs: 添加知识债功能运作说明
# 5469955 docs: 添加补充实施完成报告
# a6315cf feat(A2): 实现交卷后复盘强提醒
```

## ✅ 验收确认

完成以上验证后，请确认：

- [ ] 知识债页签可见
- [ ] 债务列表显示87笔
- [ ] 背景色为暖色调
- [ ] 出题按钮跳转Hermes正常
- [ ] Hermes自动接收指令
- [ ] 交卷后显示复盘提醒
- [ ] 所有功能流程完整

## 🎯 下一步

验证通过后：
1. 清偿第1笔债务（选择Top 5中的任意一笔）
2. 体验完整的出题→做题→复盘→清偿流程
3. 观察画像和掌握度的变化

---

**最新构建时间：** 2026-09-23 14:57  
**Git Commit：** c7bb074  
**服务器地址：** http://106.53.94.27:5173
